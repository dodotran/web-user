export default function Layout({ children }: { children: React.ReactNode }) {
  // const queryClient = useQueryClient()
  // const meData = queryClient.getQueriesData({
  //   queryKey: ['ME'],
  // })

  // if (!meData) {
  //   redirect('/')
  // }

  return children
}
